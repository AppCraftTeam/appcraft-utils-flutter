name: appcraft_utils_flutter
description: Набор утилит и расширений для Flutter/Dart-проектов команды AppCraft — исключения, расширения, инпуты, локализация, мапперы, модели, нотификаторы и диспатчер загрузки списков.
version: 0.1.0
repository: https://github.com/AppCraftTeam/appcraft-utils-flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  async: ^2.11.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter

dev_dependencies:
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
  test: ^1.24.0
